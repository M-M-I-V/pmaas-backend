<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.3</version>
		<relativePath/>
	</parent>

	<groupId>dev.mmiv</groupId>
	<artifactId>pmaas</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>pmaas</name>
	<description>An Intelligent Patient Management and Analytics System for Medical and Dental Clinics of MCST</description>
	<url/>
	<licenses><license/></licenses>
	<developers><developer/></developers>
	<scm><connection/><developerConnection/><tag/><url/></scm>

	<!--
	VERSION OVERRIDES — CVE remediation floor pins
	These override the versions provided by the Spring Boot 4.0.3 BOM.
	If the BOM ever ships a patched version higher than these, Maven's
	nearest-wins rule means the BOM wins — these are floors, not ceilings.
	Verify resolution with: mvn dependency:tree | grep -E "spring-security-web|spring-web|spring-webmvc"
	-->
	<properties>
		<java.version>21</java.version>

		<!--
		CVE-2026-22732 (CVSS 9.1) — HTTP response headers may not be written in servlet apps.
		CRITICAL for this application: CookieUtil, JwtAuthEntryPoint, and WebSecurityConfiguration
		all rely on Set-Cookie and security response headers being written reliably.
		Silent header loss in a PHI system = authentication bypass / cookie loss.
		Fixed in: Spring Security 7.0.4
		Scan confirmed: spring-security-web:7.0.3 was resolving from the BOM.
		-->
		<spring-security.version>7.0.4</spring-security.version>

		<!--
		CVE-2026-22737 (CVSS 5.9) — Script template view path traversal.
		CVE-2026-22735 (CVSS 2.6) — SSE stream corruption.
		Both fixed in Spring Framework 7.0.6.
		NOTE: 7.0.11 (previously set) does not exist in Maven Central as of this writing.
		Maven silently fell back to the BOM-managed 7.0.5, leaving CVEs unpatched.
		7.0.6 is the minimum confirmed-available safe version.
		Scan confirmed: spring-webmvc:7.0.5 and spring-web:7.0.5 were resolving.
		-->
		<spring-framework.version>7.0.6</spring-framework.version>
	</properties>

	<!--
	JACKSON NOTE:
	Spring Boot 4 uses Jackson 3 under the new groupId 'tools.jackson'.
	Only the tools.jackson BOM is declared here. The com.fasterxml.jackson
	(Jackson 2) BOM has been REMOVED — having both BOMs in dependencyManagement
	caused class resolution conflicts at runtime because Spring Boot 4's
	auto-configured ObjectMapper is Jackson 3, which does not recognise
	com.fasterxml.jackson annotations from a different package hierarchy.

	JJWT (0.12.6) bundles its own Jackson 2 serialiser internally via jjwt-jackson.
	It does not use the application's ObjectMapper, so there is no conflict.
	If JJWT Jackson 3 support becomes available, prefer it.
	-->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>tools.jackson</groupId>
				<artifactId>jackson-bom</artifactId>
				<version>3.1.0</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<!--
			com.fasterxml.jackson:jackson-bom REMOVED.
			Reason: Spring Boot 4 uses Jackson 3 (tools.jackson). Declaring the
			Jackson 2 BOM alongside the Jackson 3 BOM caused the auto-configured
			ObjectMapper to receive conflicting version signals and made Jackson 2
			annotation packages (com.fasterxml.jackson.annotation.*) appear on the
			classpath alongside Jackson 3. Source files using com.fasterxml annotations
			(e.g. @JsonFormat, @JsonIgnore) were silently ignored at runtime.
			-->
		</dependencies>
	</dependencyManagement>

	<dependencies>

		<!-- Core Spring Boot starters -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-client</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<!-- Database -->
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Utilities -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-csv</artifactId>
			<version>1.10.0</version>
		</dependency>
		<dependency>
			<groupId>com.bucket4j</groupId>
			<artifactId>bucket4j-core</artifactId>
			<version>8.10.1</version>
		</dependency>
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>2.16.1</version>
		</dependency>
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
			<version>5.3.0</version>
		</dependency>

		<!--
		Jackson 3 databind — explicitly declared so tools.jackson.annotation.*
		is available for @JsonFormat, @JsonIgnore, etc. in source files.
		Version managed by tools.jackson:jackson-bom above.
		The com.fasterxml.jackson.core:jackson-annotations dep has been REMOVED.
		All Jackson annotations must now use the tools.jackson.annotation package.
		-->
		<dependency>
			<groupId>tools.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>

		<!-- JWT -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.6</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Test -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<version>1.21.4</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>1.21.4</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.2.5</version>
				<configuration>
					<argLine>-XX:+EnableDynamicAgentLoading</argLine>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!--
			OWASP Dependency Check — run with: mvn dependency-check:check
			Bound to verify phase so `mvn verify` always includes the scan.
			Fails build on CVSS >= 7.0. Free NVD API key:
			https://nvd.nist.gov/developers/request-an-api-key
			Set as NVD_API_KEY environment variable — never hardcode here.
			-->
			<plugin>
				<groupId>org.owasp</groupId>
				<artifactId>dependency-check-maven</artifactId>
				<version>10.0.4</version>
				<configuration>
					<failBuildOnCVSS>7</failBuildOnCVSS>
					<nvdApiKey>${env.NVD_API_KEY}</nvdApiKey>
					<skipProvidedScope>false</skipProvidedScope>
					<skipTestScope>false</skipTestScope>
					<suppressionFile>dependency-check-suppressions.xml</suppressionFile>
					<outputDirectory>${project.build.directory}/dependency-check-report</outputDirectory>
					<format>HTML</format>
				</configuration>
				<executions>
					<execution>
						<goals><goal>check</goal></goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>